interface Navigable {
  /**
   * Navigates to the specified URL.
   * @param {string} url - The URL to navigate to.
   * @returns {void} Navigates to the URL and returns nothing.
   */
  navigateToUrl(url: string): void;
}
